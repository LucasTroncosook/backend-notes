{
  "name": "backend",
  "version": "1.0.0",
  "description": "Un backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "nodemon --inspect index.js",
    "build:ui": "powershell Remove-Item -Recurse -Force dist && cd ../notes-frontend && npm run build && powershell Copy-Item dist -Recurse ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m unbuild && git push"
  },
  "author": "Lucas Troncoso",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
